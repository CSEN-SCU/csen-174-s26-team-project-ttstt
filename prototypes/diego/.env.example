# TTSTT Prototype — copy to .env and adjust as needed
# Whisper model size: tiny, base, small, medium, large-v3
WHISPER_MODEL=base
# Default TTS voice (edge-tts). Run `edge-tts --list-voices` to see all options.
TTS_VOICE=en-US-AriaNeural
# Port for the FastAPI backend
BACKEND_PORT=8000
