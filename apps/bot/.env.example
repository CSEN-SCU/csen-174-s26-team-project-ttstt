# Copy to repo-root `.env` or `apps/bot/.env` (gitignored). Never commit real secrets.

DISCORD_TOKEN=
# Cloud TTS (Deepgram Aura). Required unless Piper-only (PIPER_MODEL_DIR set).
DEEPGRAM_API_KEY=
# Local Docker Postgres (infra/docker-compose.yml):
DATABASE_URL=postgresql://app:app@localhost:5432/app
# Optional — Piper (local) TTS: directory of .onnx voice models + piper CLI on PATH
# PIPER_MODEL_DIR=/home/terni/piper-voices
# PIPER_EXECUTABLE=piper
# PIPER_DEFAULT_VOICE=en_US-lessac-medium
# Optional:
FFMPEG_EXECUTABLE=ffmpeg
OPENAI_API_KEY=
# Public help site (GitHub Pages); used by /help
HELP_URL=https://csen-scu.github.io/csen-174-s26-team-project-ttstt/
