# API
apps/api/.venv/
apps/api/.env
**/__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.coverage
coverage.xml
htmlcov/
dist/
build/

# Web (root repo: keep even if apps/web has its own .gitignore)
apps/web/node_modules/
apps/web/dist/
apps/web/coverage/
apps/web/.nyc_output/

# OS / IDE
.DS_Store
.idea/
.vscode/

# Repo-level secrets (optional shared env at root)
.env
.env.local
.env.*.local

# Piper / HF / TTS caches (when added)
.cache/
models/
apps/api/data/
apps/api/models/

# Vendor desktop build (if someone compiles Mumble in-tree)
vendor/mumble/build/
vendor/mumble/**/CMakeFiles/
vendor/mumble/**/CMakeCache.txt
